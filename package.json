{
  "name": "siteproxy",
  "bin": "index.js",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "wrangler-login": "wrangler login",
    "deploy-cf-worker": "echo 'Deploy to Cloudflare with: wrangler deploy --config wrangler.jsonc' && wrangler deploy --config wrangler.jsonc",
    "deploy-cf-page": "echo 'Deploy to Cloudflare with: wrangler pages deploy' && wrangler pages deploy",
    "start": "node bundle.cjs"
  },
  "dependencies": { },
  "devDependencies": {
    "wrangler": "^4.25.1"
  }
}
