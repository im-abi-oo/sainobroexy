{
  "name": "siteproxy",
  "bin": "index.js",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "wrangler-login": "wrangler login",
    "deploy-cf-worker": "echo 'Deployment failed (Pages is required).'", 
    
    "deploy-cf-page": "echo 'Starting Cloudflare Pages deployment...' && wrangler pages deploy", 
    
    "start": "node bundle.cjs"
  },
  "dependencies": { },
  "devDependencies": {
    "wrangler": "^4.25.1"
  }
}
